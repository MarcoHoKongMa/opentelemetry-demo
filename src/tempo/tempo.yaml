server:
  http_listen_port: 3200

# distributor:
#   receivers:
#     otlp:
#       protocols:
#         grpc:
#         http:

memberlist:
  abort_if_cluster_join_fails: false
  bind_port: 7946
  join_members:
  - tempo-ingester0:7946
  - tempo-ingester1:7946
  - tempo-ingester2:7946

compactor:
  compaction:
    block_retention: 24h

querier:
  frontend_worker:
    frontend_address: tempo-query-frontend:9095

storage:
  trace:
    backend: gcs
    gcs:
      bucket_name: "tempo-sbx"
      chunk_buffer_size: 10485760